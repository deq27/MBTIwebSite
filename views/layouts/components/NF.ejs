<% for (let i = 0; i < NF.length; i += 4) { %>
<tr>
  <% for (let j = i; j < i + 4; j++) { %>
  <td>
    <% if (NF[j]) { %>
    <a href="/post/<%= NF[j]._id %>">
      <span><%= NF[j].category %></span>
    </a>
    <% } %>
  </td>
  <% } %>
</tr>
<% } %>
