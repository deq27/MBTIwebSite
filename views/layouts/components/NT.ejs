<% for (let i = 0; i < NT.length; i += 4) { %>
<tr>
  <% for (let j = i; j < i + 4; j++) { %>
  <td>
    <% if (NT[j]) { %>
    <a href="/post/<%= NT[j]._id %>">
      <span><%= NT[j].category %></span>
    </a>
    <% } %>
  </td>
  <% } %>
</tr>
<% } %>
